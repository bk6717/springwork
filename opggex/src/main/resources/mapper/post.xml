<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cos.opggex.repository.PostRepository">

<select id="findById" resultType = "com.cos.opggex.model.Post">
	SELECT * FROM post WHERE id = #{id}
</select>

</mapper>